# Port: Solicitudes de servicio (`insurance.service` 1.1.0)

Generado con `pnpm store:port insurance-service` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/service
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
