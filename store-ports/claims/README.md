# Port: Siniestros (`insurance.claims` 1.1.0)

Generado con `pnpm store:port insurance-claims` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/claims
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
