# Port: Requisitos documentales (`insurance.documents` 1.1.0)

Generado con `pnpm store:port insurance-documents` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/documents
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
