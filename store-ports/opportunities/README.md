# Port: Oportunidades (`insurance.opportunities` 1.1.0)

Generado con `pnpm store:port insurance-opportunities` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/opportunities
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
