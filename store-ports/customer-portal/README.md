# Port: Portal del cliente (`insurance.customer-portal` 1.1.0)

Generado con `pnpm store:port insurance-customer-portal` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/customer-portal
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
