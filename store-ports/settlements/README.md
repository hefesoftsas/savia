# Port: Liquidaciones (`insurance.settlements` 1.1.0)

Generado con `pnpm store:port insurance-settlements` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/settlements
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
