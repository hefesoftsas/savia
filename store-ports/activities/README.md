# Port: Actividades (`insurance.activities` 1.1.0)

Generado con `pnpm store:port insurance-activities` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/activities
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
