# Port: Emisiones (`insurance.issuance` 1.1.0)

Generado con `pnpm store:port insurance-issuance` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/issuance
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
