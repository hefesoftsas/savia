# Port: Movimientos de póliza (`insurance.endorsements` 1.1.0)

Generado con `pnpm store:port insurance-endorsements` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/endorsements
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
