# Port: Campañas (`insurance.campaigns` 1.1.0)

Generado con `pnpm store:port insurance-campaigns` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/campaigns
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
