# Port: Comunicaciones (`insurance.communications` 1.1.0)

Generado con `pnpm store:port insurance-communications` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/communications
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
