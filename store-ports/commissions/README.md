# Port: Comisiones (`insurance.commissions` 1.1.0)

Generado con `pnpm store:port insurance-commissions` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/commissions
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
