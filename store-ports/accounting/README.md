# Port: Contabilidad (`insurance.accounting` 1.1.0)

Generado con `pnpm store:port insurance-accounting` desde el release 1.0.0.

```bash
pnpm store:pack store-ports/accounting
```

Si cambia el paquete original, regenera el port y vuelve a empaquetar.
